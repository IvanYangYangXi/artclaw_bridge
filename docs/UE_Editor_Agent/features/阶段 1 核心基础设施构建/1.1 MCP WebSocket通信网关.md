# 阶段 1.1：MCP WebSocket 通信网关实现

**目标**：实现基于 WebSocket 的 MCP 通信服务器，支持自动端口探测和断线自动重连，为 AI 与 UE 编辑器之间建立稳定的双向通信通道。

**🚩 里程碑**：
1. WebSocket 服务器在插件启动时自动启动，监听可用端口（默认 8080）
2. 支持 AI 客户端连接并发送 MCP 协议消息
3. 断线后自动重连，无需手动重启服务器
4. 在 Output Log 中显示连接状态变化日志

**适用范围**：☑ UE_Editor_Agent ☐ Maya_Editor_Agent ☐ 其他

---

## 1. 背景与上下文

在阶段 0 中，我们已经完成了环境初始化和日志系统。阶段 1 的核心目标是建立 AI 与 UE 编辑器之间的通信桥梁。MCP（Model Context Protocol）协议使用 WebSocket 作为传输层，因此我们需要在 UE 插件中实现一个 WebSocket 服务器。

**关键挑战**：
- UE 编辑器的 Python 环境在后台线程运行，需要正确处理异步 IO
- 端口冲突问题（8080 可能被占用）
- 断线重连机制（AI 客户端可能意外断开）
- 线程安全（WebSocket 回调需要与 UE 主线程同步）

---

## 2. 详细实现步骤

### 2.1 创建 MCP 服务器主类

在 `Content/Python/` 目录下创建 `mcp_server.py` 文件，实现 WebSocket 服务器。

**操作步骤**：
1. 在 `Content/Python/` 目录创建 `mcp_server.py`
2. 导入必要的库（asyncio、websockets、unreal）
3. 创建 `MCPServer` 类，封装服务器逻辑
4. 实现端口探测功能，自动寻找可用端口

**注意事项**：
- 使用 `asyncio` 处理异步任务，避免阻塞 UE 主线程
- 使用 `unreal.log` 输出日志到 UE Output Log
- 捕获所有异常，防止服务器崩溃影响编辑器稳定性

### 2.2 集成到 UE 编辑器生命周期

修改 `init_unreal.py`，在编辑器启动时自动启动 MCP 服务器。

**操作步骤**：
1. 在 `init_unreal.py` 中导入 `MCPServer`
2. 在 `start_mcp_server()` 函数中创建服务器实例
3. 使用 `asyncio.create_task` 启动异步任务
4. 在编辑器关闭时清理资源

**注意事项**：
- 使用 `unreal.register_slate_post_tick_callback` 在 Slate 渲染循环中处理异步任务
- 确保在编辑器关闭时正确停止服务器，避免端口占用

### 2.3 实现断线自动重连

在 `MCPServer` 中添加重连逻辑，当 AI 客户端断开连接时自动清理并等待新的连接。

**操作步骤**：
1. 在 `connection_handler` 中捕获连接异常
2. 使用 `try-except` 包裹消息处理逻辑
3. 在 `finally` 块中清理连接状态
4. 使用 `asyncio.sleep` 实现重连延迟

**注意事项**：
- 记录断开原因到日志，便于排查问题
- 避免频繁重连，设置合理的重连间隔（如 1 秒）
- 区分正常关闭和异常断开

---

## 3. 验证清单

### 3.1 编译前检查
- [ ] `mcp_server.py` 文件位于 `Plugins/UEEditorAgent/Content/Python/`
- [ ] `websockets` 库已安装到 `Content/Python/Lib/`
- [ ] `init_unreal.py` 中已添加 `from mcp_server import start_mcp_server`
- [ ] C++ Subsystem 已添加 `SetServerPort` 和 `GetServerAddress` 方法
- [ ] Build.cs 中已添加必要的模块依赖（如 `HTTP` 模块）

### 3.2 功能验证
- [ ] **启动服务器**：重启UE编辑器后，Output Log 显示 "MCP服务器已启动: ws://localhost:8080"
- [ ] **端口探测**：如果8080被占用，自动使用8081、8082等可用端口
- [ ] **连接状态**：打开Dashboard窗口，初始状态显示 "Status: Offline"
- [ ] **客户端连接**：使用WebSocket测试工具连接，Dashboard状态变为 "Online"
- [ ] **消息回显**：发送测试消息，服务器返回 "UE Editor 已收到"
- [ ] **断线重连**：断开客户端后，服务器等待1秒，可以重新连接

### 3.3 高级验证
- [ ] **多客户端测试**：尝试同时连接2个客户端，确认服务器只接受一个连接
- [ ] **网络异常**：模拟网络断开（拔掉网线），服务器不崩溃，可重连
- [ ] **编辑器关闭**：关闭UE编辑器时，服务器正确停止，无端口占用残留
- [ ] **心跳机制**：连接后等待30秒，连接保持活跃未断开
- [ ] **端口范围**：故意占用8080-8180所有端口，服务器记录警告并使用8080

---

## 4. 常见问题与解决方案

### 问题1：ImportError: No module named 'websockets'

**现象**：
```
[UEAgent] 启动MCP服务器时发生错误: No module named 'websockets'
```

**原因**：
websockets 库未正确安装到插件的 Python Lib 目录

**解决方案**：
1. 检查 `Plugins/UEEditorAgent/Content/Python/Lib/websockets` 是否存在
2. 如果不存在，运行依赖安装脚本：
   ```bash
   pip install --target="D:/MyProject/ArtClaw/subprojects/UEDAgentProj/Plugins/UEEditorAgent/Content/Python/Lib" websockets
   ```
3. 重启UE编辑器

### 问题2：OSError: [WinError 10048] 通常每个套接字地址只能使用一次

**现象**：
```
[MCPServer] 启动失败: [WinError 10048] 通常每个套接字地址只能使用一次
```

**原因**：
端口被其他程序占用，且端口探测未生效

**解决方案**：
1. 检查是否有其他程序占用8080端口：
   ```bash
   netstat -ano | findstr :8080
   ```
2. 在 `init_unreal.py` 中修改起始端口：
   ```python
   start_mcp_server(host="localhost", port=9000)  # 使用其他端口
   ```
3. 检查防火墙设置，确保端口未被阻止

### 问题3：Dashboard状态不更新

**现象**：
AI客户端已连接，但Dashboard窗口仍显示 "Status: Offline"

**原因**：
1. Subsystem的委托绑定失败
2. Python端未正确调用 `set_connection_status(True)`

**解决方案**：
1. 检查 `mcp_server.py` 中的连接处理逻辑：
   ```python
   subsystem = unreal.get_editor_subsystem(unreal.UEAgentSubsystem)
   if subsystem:
       subsystem.set_connection_status(True)  # 确认这行代码存在
   ```
2. 在UE编辑器中打开Output Log，检查是否有Python错误
3. 使用Python控制台手动测试：
   ```python
   import unreal
   subsystem = unreal.get_editor_subsystem(unreal.UEAgentSubsystem)
   subsystem.set_connection_status(True)  # 手动设置状态
   ```

### 问题4：编辑器关闭时卡顿

**现象**：
关闭UE编辑器时，界面卡住数秒后关闭

**原因**：
MCP服务器未正确停止，导致异步任务阻塞

**解决方案**：
1. 确保 `mcp_server.py` 中有 `stop()` 方法
2. 在 `init_unreal.py` 中添加编辑器关闭回调：
   ```python
   @unreal.uclass()
   class OnEditorClose(unreal.EditorUtilityObject):
       @unreal.ufunction(override=True)
       def begin_destroy(self):
           from mcp_server import stop_mcp_server
           stop_mcp_server()
   ```
3. 检查是否有未处理的异步任务

### 问题5：WebSocket连接后立即断开

**现象**：
AI客户端可以连接，但几秒钟后自动断开

**原因**：
1. 心跳机制未正确配置
2. 消息处理异常导致连接关闭

**解决方案**：
1. 检查 `websockets.serve` 的心跳参数：
   ```python
   self.server = await websockets.serve(
       self.connection_handler,
       self.host,
       self.port,
       ping_interval=20,  # 确保这些参数设置
       ping_timeout=10,
   )
   ```
2. 在 `handle_message` 中添加详细的异常捕获和日志
3. 使用Wireshark或浏览器开发者工具检查网络流量

---

## 5. 跨项目差异说明

### UE_Editor_Agent 实现
- 使用 `asyncio` + `websockets` 实现异步服务器
- 集成到UE编辑器生命周期，使用 `SlatePostTick` 处理异步任务
- 通过 `UEAgentSubsystem` 管理连接状态

### 其他项目（未来）

#### Maya_Editor_Agent
- 使用 `maya.utils.executeDeferred` 处理异步任务
- 集成到Maya的 `idle` 事件循环
- 通过Maya的脚本Job管理生命周期

#### Blender_Editor_Agent
- 使用 `bpy.app.timers.register` 处理异步任务
- 集成到Blender的定时器系统
- 通过Blender的插件注册/注销管理生命周期

---

## 6. 下一步建议

### 已完成
- ✅ WebSocket服务器基础框架
- ✅ 自动端口探测和断线重连
- ✅ 与UE编辑器生命周期集成
- ✅ 连接状态实时同步到UI

### 下一步（阶段 1.2）
实现**万能执行器 (Universal Proxy)**，开发核心Tool `run_ue_python(code: str)`，支持执行任意Python代码并捕获异常。

**关键要点**：
- 使用 `exec()` 函数执行代码
- 内置 `try-except` 捕获所有异常
- 将Traceback转换为字符串返回
- 集成到MCP工具列表

---

## 7. 变更历史

| 日期 | 版本 | 变更内容 | 作者 |
| :--- | :--- | :--- | :--- |
| 2026-03-14 | 1.0 | 初始版本，实现WebSocket通信网关 | AI助手 |

---

**🚩 里程碑完成标准**：
1. ✅ WebSocket服务器在插件启动时自动启动，监听可用端口（默认8080）
2. ✅ AI客户端可以连接并发送MCP协议消息
3. ✅ 断线后自动重连，无需手动重启服务器
4. ✅ Output Log中显示连接状态变化日志
3. ✅ Dashboard窗口实时显示连接状态（Online/Offline）

**文档版本**：1.0
**最后更新**：2026-03-14
**适用代码版本**：1.0.0-alpha
**维护者**：AI助手