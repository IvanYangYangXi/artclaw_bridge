# 阶段 0.3：开发者菜单与可停靠窗口 (Tab Window) 详细实现

**目标**：在UE编辑器中创建可停靠的Dashboard窗口，并集成到工具栏，实现与UUEAgentSubsystem的状态实时同步。

**🚩 里程碑**：
1. UE编辑器顶部工具栏出现Agent图标按钮
2. 点击按钮弹出可停靠的Dashboard窗口，支持拖拽吸附
3. 窗口实时显示插件版本号、连接状态、统计信息
4. 状态变更时UI自动刷新（无需手动关闭重开）

---

### 1. 核心常量与样式定义

#### 1.1 创建常量头文件 (UEAgentConstants.h)
在 `Source/UEEditorAgent/Public/` 目录下创建：
```cpp
#pragma once
#include "CoreMinimal.h"

// Tab ID常量
static const FName UEAgentTabName(TEXT("UEAgentDashboard"));

// 工具栏按钮样式名
static const FName UEAgentToolbarStyleName(TEXT("UEAgent.ToolbarButton"));
```

#### 1.2 创建样式管理类 (UEEditorAgentStyle.h)
在 `Source/UEEditorAgent/Public/` 目录下创建：
```cpp
#pragma once

#include "CoreMinimal.h"
#include "Styling/SlateStyle.h"

class UEEDITORAGENT_API FUEEditorAgentStyle
{
public:
	static void Initialize();
	static void Shutdown();
	static TSharedPtr<ISlateStyle> Get() { return StyleSet; }

private:
	static TSharedPtr<FSlateStyleSet> StyleSet;
	static FString InContent(const FString& RelativePath, const ANSICHAR* Extension);
};
```

#### 1.3 实现样式注册 (UEEditorAgentStyle.cpp)
在 `Source/UEEditorAgent/Private/` 目录下创建：
```cpp
#include "UEEditorAgentStyle.h"
#include "Styling/SlateStyleRegistry.h"
#include "Styling/SlateTypes.h"
#include "Styling/CoreStyle.h"
#include "EditorStyleSet.h"

TSharedPtr<FSlateStyleSet> FUEEditorAgentStyle::StyleSet = nullptr;

void FUEEditorAgentStyle::Initialize()
{
	if (!StyleSet.IsValid())
	{
		StyleSet = MakeShareable(new FSlateStyleSet(TEXT("UEEditorAgentStyle")));
		StyleSet->SetContentRoot(FPaths::ProjectPluginsDir() / TEXT("UEEditorAgent/Resources"));

		// 注册工具栏按钮图标（40x40像素，SVG或PNG）
		StyleSet->Set(
			TEXT("UEAgent.ToolbarButton"),
			new FSlateImageBrush(
				StyleSet->RootToContentDir(TEXT("Icon128")), // 使用Resources/Icon128.png
				FVector2D(40.0f, 40.0f)
			)
		);

		FSlateStyleRegistry::RegisterSlateStyle(*StyleSet);
	}
}

void FUEEditorAgentStyle::Shutdown()
{
	if (StyleSet.IsValid())
	{
		FSlateStyleRegistry::UnRegisterSlateStyle(*StyleSet);
		StyleSet.Reset();
	}
}
```

---

### 2. Slate UI与实时刷新实现

---

### 2. 插件模块逻辑 (FUEAgentModule.cpp)
你需要在 `StartupModule` 中完成两件事：注册 **Tab 产生器 (Tab Spawner)** 和 **工具栏按钮**。

#### 2.1 注册 Tab Spawner
```cpp
#include "Widgets/Docking/SDockTab.h"
#include "Widgets/Text/STextBlock.h"
#include "UEAgentConstants.h"

void FUEAgentModule::StartupModule()
{
    // 1. 注册样式（见上一步实现）
    FUEAgentStyle::Initialize();

    // 2. 注册 Tab 产生器
    FGlobalTabmanager::Get()->RegisterNomadTabSpawner(UEAgentTabName,
        FOnSpawnTab::CreateRaw(this, &FUEAgentModule::OnSpawnPluginTab))
        .SetDisplayName(FText::FromString("UE Editor Agent"))
        .SetMenuType(ETabSpawnerMenuType::Enabled);

    // 3. 注册工具栏按钮 (UToolMenus 逻辑同上)
    UToolMenus::RegisterStartupCallback(FSimpleMulticastDelegate::FDelegate::CreateRaw(this, &FUEAgentModule::RegisterMenus));
}

// 2.1 定义Slate Widget（支持实时刷新）
class SUEAgentDashboardWidget : public SCompoundWidget
{
public:
	SLATE_BEGIN_ARGS(SUEAgentDashboardWidget) {}
	SLATE_END_ARGS()

	void Construct(const FArguments& InArgs)
	{
		// 获取子系统单例
		Subsystem = GEditor->GetEditorSubsystem<UUEAgentSubsystem>();

		// 初始化文本绑定
		StatusText = SNew(STextBlock);
		VersionText = SNew(STextBlock).Text(FText::FromString(TEXT("Plugin Version: 1.0.0-alpha")));
		StatsText = SNew(STextBlock).Text(FText::FromString(TEXT("Commands Executed: 0")));

		// 构建UI布局
		ChildSlot
		[
			SNew(SVerticalBox)
			// 标题区域
			+ SVerticalBox::Slot().AutoHeight().Padding(16, 16, 16, 8)
			[
				SNew(STextBlock)
				.Text(FText::FromString(TEXT("UE Editor Agent Dashboard")))
				.Font(FCoreStyle::GetDefaultFontStyle("Bold", 18))
			]

			// 状态信息区域
			+ SVerticalBox::Slot().AutoHeight().Padding(16, 8, 16, 8)
			[
				SNew(SVerticalBox)
				+ SVerticalBox::Slot().AutoHeight().Padding(0, 2)
				[
					VersionText.ToSharedRef()
				]
				+ SVerticalBox::Slot().AutoHeight().Padding(0, 2)
				[
					StatusText.ToSharedRef()
				]
				+ SVerticalBox::Slot().AutoHeight().Padding(0, 2)
				[
					StatsText.ToSharedRef()
				]
			]

			// 分割线
			+ SVerticalBox::Slot().AutoHeight().Padding(16, 8, 16, 8)
			[
				SNew(SSeparator)
			]

			// 操作按钮区域
			+ SVerticalBox::Slot().AutoHeight().Padding(16, 8, 16, 16)
			[
				SNew(SHorizontalBox)
				+ SHorizontalBox::Slot().AutoWidth().Padding(0, 0, 8, 0)
				[
					SNew(SButton)
					.Text(FText::FromString(TEXT("Test Connection")))
					.OnClicked(this, &SUEAgentDashboardWidget::OnTestConnection)
				]
				+ SHorizontalBox::Slot().AutoWidth().Padding(8, 0, 0, 0)
				[
					SNew(SButton)
					.Text(FText::FromString(TEXT("View Logs")))
					.OnClicked(this, &SUEAgentDashboardWidget::OnViewLogs)
				]
			]
		];

		// 绑定状态变更委托（实时刷新核心）
		if (Subsystem)
		{
			Subsystem->OnConnectionStatusChanged.AddSP(this, &SUEAgentDashboardWidget::OnConnectionStatusChanged);
			UpdateStatusText(Subsystem->GetConnectionStatus());
		}
	}

	~SUEAgentDashboardWidget()
	{
		if (Subsystem)
		{
			Subsystem->OnConnectionStatusChanged.RemoveAll(this);
		}
	}

private:
	// 状态变更回调（实时刷新）
	void OnConnectionStatusChanged(bool bIsConnected)
	{
		UpdateStatusText(bIsConnected);
	}

	void UpdateStatusText(bool bIsConnected)
	{
		FString Status = bIsConnected ? TEXT("Online") : TEXT("Offline");
		FLinearColor Color = bIsConnected ? 
			FLinearColor::Green : FLinearColor::Red;

		StatusText->SetText(FText::FromString(FString::Printf(TEXT("Status: %s"), *Status)));
		StatusText->SetColorAndOpacity(FSlateColor(Color));
	}

	FReply OnTestConnection()
	{
		if (Subsystem)
		{
			Subsystem->SetConnectionStatus(!Subsystem->GetConnectionStatus());
		}
		return FReply::Handled();
	}

	FReply OnViewLogs()
	{
		FGlobalTabmanager::Get()->TryInvokeTab(FName("OutputLog"));
		return FReply::Handled();
	}

private:
	TWeakObjectPtr<UUEAgentSubsystem> Subsystem;
	TSharedPtr<STextBlock> StatusText;
	TSharedPtr<STextBlock> VersionText;
	TSharedPtr<STextBlock> StatsText;
};

// 2.2 Tab Spawner 绑定
TSharedRef<SDockTab> FUEEditorAgentModule::OnSpawnPluginTab(const FSpawnTabArgs& SpawnTabArgs)
{
	return SNew(SDockTab)
		.TabRole(ETabRole::NomadTab)
		[
			SNew(SUEAgentDashboardWidget)
		];
}
```

### 3. 工具栏按钮注册 (FUEEditorAgentModule)

#### 3.1 创建命令定义 (UEEditorAgentCommands.h)
```cpp
#pragma once

#include "CoreMinimal.h"
#include "Framework/Commands/Commands.h"

class UEEDITORAGENT_API FUEEditorAgentCommands : public TCommands<FUEEditorAgentCommands>
{
public:
	FUEEditorAgentCommands() : TCommands<FUEEditorAgentCommands>(
		TEXT("UEEditorAgent"),
		FText::FromString(TEXT("UE Editor Agent Plugin")),
		NAME_None,
		FUEEditorAgentStyle::Get()->GetStyleSetName())
	{
	}

	virtual void RegisterCommands() override;

public:
	TSharedPtr<FUICommandInfo> OpenAgentDashboard;
};
```

#### 3.2 实现命令注册 (UEEditorAgentCommands.cpp)
```cpp
#include "UEEditorAgentCommands.h"

#define LOCTEXT_NAMESPACE "FUEEditorAgentCommands"

void FUEEditorAgentCommands::RegisterCommands()
{
	UI_COMMAND(OpenAgentDashboard, "UE Agent", "Open UE Editor Agent Dashboard",
		EUserInterfaceActionType::Button,
		FInputGesture());
}

#undef LOCTEXT_NAMESPACE
```

#### 3.3 模块启动与菜单注册 (UEEditorAgent.cpp)
```cpp
#include "UEEditorAgent.h"
#include "UEEditorAgentStyle.h"
#include "UEEditorAgentCommands.h"
#include "UEAgentConstants.h"
#include "UEAgentSubsystem.h"
#include "LevelEditor.h"
#include "Widgets/Docking/SDockTab.h"
#include "Framework/Docking/TabManager.h"
#include "ToolMenus.h"

static const FName UEAgentTabName(TEXT("UEAgentDashboard"));

#define LOCTEXT_NAMESPACE "FUEEditorAgentModule"

void FUEEditorAgentModule::StartupModule()
{
	// 1. 初始化样式
	FUEEditorAgentStyle::Initialize();
	FUEEditorAgentStyle::ReloadTextures();

	// 2. 注册命令
	FUEEditorAgentCommands::Register();

	PluginCommands = MakeShareable(new FUICommandList);
	PluginCommands->MapAction(
		FUEEditorAgentCommands::Get().OpenAgentDashboard,
		FExecuteAction::CreateRaw(this, &FUEEditorAgentModule::OnOpenDashboard),
		FCanExecuteAction());

	// 3. 注册Tab Spawner
	FGlobalTabmanager::Get()->RegisterNomadTabSpawner(
		UEAgentTabName,
		FOnSpawnTab::CreateRaw(this, &FUEEditorAgentModule::OnSpawnPluginTab))
		.SetDisplayName(LOCTEXT("TabTitle", "UE Editor Agent"))
		.SetTooltipText(LOCTEXT("TooltipText", "Open the UE Editor Agent dashboard tab."))
		.SetIcon(FSlateIcon(FUEEditorAgentStyle::GetStyleSetName(), "UEAgent.ToolbarButton"));

	// 4. 注册菜单扩展
	UToolMenus::RegisterStartupCallback(FSimpleMulticastDelegate::FDelegate::CreateRaw(
		this, &FUEEditorAgentModule::RegisterMenus));
}

void FUEEditorAgentModule::ShutdownModule()
{
	UToolMenus::UnRegisterStartupCallback(this);
	UToolMenus::UnregisterOwner(this);

	FUEEditorAgentStyle::Shutdown();
	FUEEditorAgentCommands::Unregister();
}

void FUEEditorAgentModule::RegisterMenus()
{
	FToolMenuOwnerScoped OwnerScoped(this);

	// 添加到 "Window" 主菜单
	{
		UToolMenu* Menu = UToolMenus::Get()->ExtendMenu("LevelEditor.MainMenu.Window");
		FToolMenuSection& Section = Menu->FindOrAddSection("WindowLayout");
		Section.AddMenuEntryWithCommandList(FUEEditorAgentCommands::Get().OpenAgentDashboard, PluginCommands);
	}

	// 添加到工具栏
	{
		UToolMenu* ToolbarMenu = UToolMenus::Get()->ExtendMenu("LevelEditor.LevelEditorToolBar");
		FToolMenuSection& Section = ToolbarMenu->FindOrAddSection("Settings");
		FToolMenuEntry& Entry = Section.AddEntry(FToolMenuEntry::InitToolBarButton(
			FUEEditorAgentCommands::Get().OpenAgentDashboard));
		Entry.SetCommandList(PluginCommands);
	}
}

void FUEEditorAgentModule::OnOpenDashboard()
{
	FGlobalTabmanager::Get()->TryInvokeTab(UEAgentTabName);
}

TSharedRef<SDockTab> FUEEditorAgentModule::OnSpawnPluginTab(const FSpawnTabArgs& SpawnTabArgs)
{
	return SNew(SDockTab)
		.TabRole(ETabRole::NomadTab)
		[
			SNew(SUEAgentDashboardWidget)
		];
}

#undef LOCTEXT_NAMESPACE

IMPLEMENT_MODULE(FUEEditorAgentModule, UEEditorAgent)
```

---

### 4. 配置参数表

| 配置项 | 推荐值 | 说明 |
| :--- | :--- | :--- |
| **图标尺寸** | 40x40 像素 | 工具栏按钮标准尺寸，支持SVG或PNG |
| **Tab ID** | `UEAgentDashboard` | 全局唯一，用于Tab管理器识别 |
| **窗口标题** | "UE Editor Agent" | 显示在Tab标签上的文本 |
| **初始化时机** | `PostEngineInit` | 确保Python环境已准备好 |
| **UI刷新方式** | Delegate绑定 | 比轮询更高效，响应更及时 |

---

### 5. 完整验证清单

#### 5.1 编译前检查
- [ ] 所有头文件包含正确（`#pragma once`）
- [ ] 模块依赖已添加（`Slate`, `SlateCore`, `ToolMenus`）
- [ ] 图标文件 `Resources/Icon128.png` 存在（40x40px）
- [ ] Build.cs文件中添加了 `FUEEditorAgentStyle` 和 `FUEEditorAgentCommands` 源文件

#### 5.2 功能验证
- [ ] **编译通过**：插件无报错编译成功
- [ ] **图标显示**：工具栏出现Agent图标，样式与UE一致
- [ ] **窗口弹出**：点击图标弹出Dashboard窗口
- [ ] **可停靠**：窗口可以拖拽吸附到Content Browser、Details等面板旁
- [ ] **信息展示**：窗口显示版本号、连接状态、统计信息
- [ ] **实时刷新**：通过Python调用`subsystem.set_connection_status()`时，UI立即更新
- [ ] **按钮功能**："Test Connection"按钮能切换状态，"View Logs"能打开Output Log

#### 5.3 高级验证
- [ ] **委托绑定**：在Python中多次修改状态，UI都能正确响应
- [ ] **内存安全**：反复打开关闭窗口，无内存泄漏
- [ ] **编辑器重启**：重启UE后，插件自动加载，状态恢复初始值
- [ ] **多窗口**：同时打开多个编辑器窗口时，状态同步正确

---

### 6. 样式与图标规范

#### 6.1 图标要求
- **格式**：SVG（推荐）或PNG
- **尺寸**：40x40 像素
- **颜色**：遵循UE编辑器配色方案（暗色主题）
- **位置**：`Plugins/UEEditorAgent/Resources/Icon128.png`

#### 6.2 Slate样式最佳实践
- 使用UE标准的内边距（`Padding(16, 8)`）
- 标题字体使用 `Bold, 18`
- 状态文本使用默认字体，通过颜色区分状态
- 按钮保持水平间距（`Padding(0, 0, 8, 0)`）

---

### 7. 下一步建议

**已完成**：
- ✅ Slate UI容器和实时刷新机制
- ✅ 工具栏集成
- ✅ 状态显示和同步

**下一步（阶段 0.4）**：
进入日志系统实现，将Python日志重定向到UE Output Log，并支持多级日志分类。

---

**🚩 里程碑完成标准**：
1. ✅ 编译通过后，UE编辑器顶部工具栏出现Agent图标按钮
2. ✅ 点击按钮能成功弹出可停靠的Dashboard窗口
3. ✅ 窗口显示版本号、连接状态、统计信息
4. ✅ 状态变更时UI自动刷新，无需手动关闭重开
5. ✅ 窗口内的按钮功能正常工作

**提示**：C++修改后需要重新编译。如果使用Live Coding，在修改`.h`文件或注册新类时，需要重启编辑器以确保单例正确注册。
