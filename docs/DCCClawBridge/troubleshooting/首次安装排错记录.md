# DCCClawBridge 首次安装与调试排错记录

> 版本: v0.2.0 | 日期: 2026-03-19

## 问题索引

| # | 问题 | 严重性 | 状态 | 日期 |
|---|------|--------|------|------|
| 1 | install.bat LF 换行导致 cmd 解析失败 | 严重 | ✅ 已修复 | 2026-03-19 |
| 2 | echo 行 `→`/`->` 被当成重定向 | 中等 | ✅ 已修复 | 2026-03-19 |
| 3 | Maya 中文版安装到错误的 scripts 目录 | 严重 | ✅ 已修复 | 2026-03-19 |
| 4 | chat_panel.py QFlowLayout 语法错误 | 严重 | ✅ 已修复 | 2026-03-19 |
| 5 | bridge_core 裸导入失败 | 严重 | ✅ 已修复 | 2026-03-19 |
| 6 | Maya Python 缺少 websockets 依赖 | 严重 | ✅ 已修复 | 2026-03-19 |
| 7 | Agent 不识别 DCC 环境 | 中等 | ✅ 已修复 | 2026-03-19 |
| 8 | 快捷按钮 lambda checked 参数报错 | 中等 | ✅ 已修复 | 2026-03-19 |
| 9 | 流式更新冲掉历史消息 | 中等 | ✅ 已修复 | 2026-03-19 |
| 10 | Max MCP Server 未配置 | 中等 | ✅ 已修复 | 2026-03-19 |
| 11 | setup_openclaw_config.py 不自动注入工具权限 | 低 | ✅ 已修复 | 2026-03-19 |
| 12 | displayName 硬编码为 "UE Claw Bridge" | 中等 | ✅ 已修复 | 2026-03-19 |
| 13 | "新对话"按钮未真正创建新 session | 严重 | ✅ 已修复 | 2026-03-19 |
| 14 | Chat Panel 窗口标题固定不显示 DCC 名称 | 低 | ✅ 已修复 | 2026-03-19 |
| 15 | 新对话的 AI 回复不可见 | 中等 | ✅ 已修复 | 2026-03-19 |

---

## 1. install.bat LF 换行导致 cmd 解析失败

**现象**: 双击 install.bat 大量乱码报错：`'鈺愨晲鈺' 不是内部或外部命令`

**根因**: bat 文件使用 Unix LF 换行，Windows cmd.exe 要求 CRLF。LF-only 导致解析错位，UTF-8 中文字节被截断后当命令执行。

**修复**: 
- LF → CRLF 转换
- 新增 `.gitattributes`: `*.bat *.cmd text eol=crlf`

**教训**: Windows bat 文件必须用 CRLF，git 可能在 checkout 时转换为 LF。

---

## 2. echo 行重定向符号

**现象**: `'3.' is not recognized`，项目根目录出现垃圾文件 `UE`, `ArtClaw`, `Chat`, `连`

**根因**: `echo ... -> UE Claw Bridge` 中 `->` 的 `>` 被 cmd 当成文件重定向。

**修复**: `→` 和 `->` 全部改为 `--`

**教训**: bat echo 行中不能有 `>`、`→`、`->` 等含 `>` 的字符。

---

## 3. Maya 中文版 locale 目录

**现象**: install.bat 安装成功但 Maya 启动无 ArtClaw 菜单

**根因**: Maya 中文版 scripts 目录是 `Documents/maya/2023/zh_CN/scripts/`，install.bat 只写 `Documents/maya/2023/scripts/`。

**修复**:
- install.bat 自动扫描 locale 子目录
- 同时安装到 scripts/ 和 locale/scripts/
- userSetup.py 加防重复初始化保护

**教训**: DCC 中文版的用户目录结构可能与英文版不同。

---

## 4. chat_panel.py 语法错误

**现象**: `invalid syntax (chat_panel.py, line 29)`

**根因**: `QFlowLayout if False else QWidget` 放在 import 语句的括号里，不是合法 Python 语法。

**修复**: 删除该行，QFlowLayout 没有实际引用。

---

## 5. bridge_core 裸导入失败

**现象**: `No module named 'bridge_core'`

**根因**: `from bridge_core import ...` 是裸导入，需要 bridge_core.py 所在目录在 sys.path 上。DCCClawBridge/ 在 sys.path 里但 core/ 不在。

**修复**: bridge_dcc.py 导入前把 `os.path.dirname(__file__)` (即 core/) 加到 sys.path。

---

## 6. websockets 依赖

**现象**: 点连接无反应，chat_panel.py 初始化 bridge 时静默失败

**根因**: Maya Python 环境是独立的，没有安装 websockets 库。

**修复**: `mayapy.exe -m pip install websockets`

**建议**: install.bat 应自动安装 Maya Python 依赖（类似 UE 部分的处理）。

---

## 7. Agent 不识别 DCC 环境

**现象**: 在 Maya 里对话，agent 以为在 UE 里

**根因**: 
1. session key 硬编码 `agent/ue-editor`
2. 消息不含 DCC 上下文信息

**修复**:
- bridge_core.py 新增 `client_id` 参数，session key 变为 `agent/{dcc}-editor`
- 各 adapter 启动时调用 `DCCBridgeManager.set_dcc_name("maya"/"max")`
- `_enrich_with_briefing()` 首条消息注入 `[DCC Context]`

---

## 8. 快捷按钮 lambda 报错

**现象**: `TypeError: <lambda>() missing 1 required positional argument: 'checked'`

**根因**: Qt `clicked` signal 在部分 PySide2 版本不传 checked 参数。

**修复**: `lambda _checked=False, c=content: ...`

---

## 9. 流式更新冲掉历史消息

**现象**: AI 回复时之前的对话记录消失

**根因**: `_update_last_message` 用 block navigation 定位最后一条消息，但 HTML 渲染后 block 数量不可控。

**修复**: 记录 AI 消息的 cursor position 锚点，流式更新时从锚点到文档末尾精确替换。

---

## 10. Max MCP Server 未配置

**现象**: Max 工具不可用

**根因**: openclaw.json 的 mcp-bridge servers 里没有 max-primary 配置。

**修复**: 添加 `max-primary: ws://127.0.0.1:8082`，工具权限加到所有 agent。

---

## 11. setup_openclaw_config.py 不自动注入工具权限

**现象**: 运行配置脚本后 agent 仍然没有 DCC MCP 工具

**根因**: 脚本只添加 server 配置，不修改 agent 的 tools.allow 列表。

**修复**: 自动根据 server name 注入对应的 MCP 工具到所有 agent 的 allow 列表。

---

## 12. displayName 硬编码为 "UE Claw Bridge"

**现象**: Maya 连接 Gateway 后，会话标题显示 "UE Claw Bridge" 而非 Maya

**根因**: `bridge_core.py` 中 `displayName` 硬编码为 `"UE Claw Bridge"`，没有根据实际 DCC 类型动态设置。

**修复** (commit f05d9e9):
- `bridge_core.py` 新增 `_DISPLAY_NAME_MAP`，根据 `client_id` 自动映射：
  - `maya-editor` → "Maya Claw Bridge"
  - `max-editor` → "Max Claw Bridge"
  - `ue-editor` → "UE Claw Bridge"
- 各 adapter 启动时通过 `DCCBridgeManager.set_dcc_name()` 设置正确的 DCC 名称

**教训**: 多 DCC 环境下所有标识性字符串都需要参数化，不能硬编码。

---

## 13. "新对话"按钮未真正创建新 session

**现象**: 点击"新对话"按钮后继续发消息，Gateway 认为是同一个 session，对话历史没有清空。

**排查过程**:
1. 最初方案：向 Gateway 发 `/new` 重置 session → 发现 `/new` 导致 Gateway 关闭 WebSocket (1000 OK)，需要重连
2. `session_key` 是惰性生成的，点新对话时可能还是 `None`，条件判断跳过清空逻辑
3. 即使 key 存在，格式固定为 `agent_id/client_id`，清掉后重新生成还是同一个 key

**根因**: session key 不变 = Gateway 认为是同一个 session。清空 key 后重新生成，如果格式不含唯一标识（如时间戳），生成的还是同一个 key。

**修复** (commit da2b5e4 + 30742ed):
- Session key 格式改为 `agent/{client_id}:{timestamp}`（如 `agent/maya-editor:1710842400`）
- 每次新对话清空 key 后，下次发送时生成含新时间戳的唯一 key
- `reset_session()` 两步走：① 清 `session_key` ② 通过 `send_message("/new")` 正常发送

**教训**:
- OpenClaw Gateway 处理 `/new` slash command 会关闭 WebSocket 连接（1000 OK），不能通过 bridge RPC 发 `/new`
- Session key 必须全局唯一才能创建真正的新 session，用时间戳后缀区分
- Python 模块 `importlib.reload()` 后，已创建实例的方法不自动更新，需要 `types.MethodType` 手动绑定

---

## 14. Chat Panel 窗口标题固定不显示 DCC 名称

**现象**: Chat Panel 窗口标题始终显示 "ArtClaw Chat"，用户无法区分是哪个 DCC 的对话面板。

**根因**: `chat_panel.py` 窗口标题硬编码。

**修复** (commit 30742ed): 窗口标题改为动态显示，格式如 "ArtClaw Chat - Maya"，根据 `DCCBridgeManager` 的 DCC 名称设置。

---

## 15. 新对话的 AI 回复不可见

**现象**: 点击"新对话"后，AI 的回复（如新会话问候语）没有显示在 Chat Panel 中。

**根因**: 旧逻辑中"新对话"只清空本地状态，不触发流式接收模式。`/new` 走的是 slash command 路径，其回复没有绑定到 Chat Panel 的流式显示回调。

**修复** (commit e4e7e96 + 30742ed):
- 点击"新对话"后设置 streaming 状态（表示正在等待 AI 回复）
- `/new` 改走正常消息路径（`send_message("/new")`），流式回调正常回传到 Chat Panel
- AI 的新会话回复完整显示在面板中

**教训**: 所有发给 Gateway 的消息都应走统一的消息发送路径，确保回调链完整。特殊命令不应绕过 UI 的流式显示机制。
