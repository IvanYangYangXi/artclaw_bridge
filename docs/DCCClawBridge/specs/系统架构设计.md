# DCCClawBridge 系统架构设计

---

## 一、整体架构

```
┌──────────────────────────────────────────────────┐
│              AI Agent 平台层                      │
│            OpenClaw（当前落地）                    │
└───────────────────┬──────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │ 上行: Gateway私有RPC   │ 下行: MCP JSON-RPC
        │ (chat / 流式响应)      │ (tool call / result)
        │                       │
┌───────▼───────────────────────▼──────────────────┐
│         OpenClaw Gateway 转发层                    │
│                                                    │
│  上行: bridge_core.py        下行: mcp-bridge 插件 │
│  (每个 DCC 进程内独立运行)    (Gateway 内, 管理     │
│   → 共享代码, 独立实例         多个 MCP 连接)      │
└───────┬───────────────────────┬──────────────────┘
        │                       │
┌───────▼───────────────────────▼──────────────────┐
│               DCCClawBridge                       │
│  ┌─────────────────────────────────────────────┐ │
│  │          artclaw_ui (Qt 通用界面)            │ │
│  │  Chat Panel · Skill Manager · Status Bar    │ │
│  ├─────────────────────────────────────────────┤ │
│  │              core (共享核心)                  │ │
│  │  bridge_dcc.py · Skill Hub · Knowledge ·    │ │
│  │  Memory · 依赖管理 · 命令队列               │ │
│  ├──────────┬──────────────┬───────────────────┤ │
│  │  Maya    │   3ds Max    │   未来 DCC…       │ │
│  │ Adapter  │   Adapter    │   Adapter         │ │
│  ├──────────┼──────────────┼───────────────────┤ │
│  │  Maya    │   Max        │                   │ │
│  │ MCP工具  │   MCP工具    │   MCP工具         │ │
│  │ + Skills │   + Skills   │   + Skills        │ │
│  └──────────┴──────────────┴───────────────────┘ │
│               DCC 原生 API / SDK                  │
└──────────────────────────────────────────────────┘
```

**关键区分**：
- **通信框架代码共享**：`bridge_core.py`、`mcp_server.py` 骨架等代码文件被所有 DCC 复用
- **运行时实例独立**：每个 DCC 进程内运行自己的 MCP Server（端口不同）、自己的 Bridge 连接、注册自己的工具集
- AI 通过工具名前缀区分目标 DCC：`mcp_maya-primary_run_python` vs `mcp_ue-editor_run_ue_python`

---

## 二、目录结构

```
subprojects/DCCClawBridge/
│
├── artclaw_ui/                  # 通用 Qt 界面
│   ├── __init__.py
│   ├── chat_panel.py            # 主聊天面板（消息列表 + 输入框 + 流式显示）
│   ├── skill_manager_panel.py   # Skill 浏览/创建/启停管理
│   ├── status_bar.py            # 连接状态 + 诊断入口
│   ├── theme.py                 # 统一主题（深色/浅色 + DCC 配色微调）
│   ├── slash_commands.py        # / 命令补全与分发
│   └── widgets/                 # 可复用 Qt 组件
│       ├── __init__.py
│       ├── message_bubble.py    # 消息气泡（支持 Markdown 渲染）
│       ├── code_block.py        # 代码块（语法高亮 + 复制按钮）
│       └── loading_indicator.py # 流式响应加载动画
│
├── adapters/                    # DCC 薄适配层
│   ├── __init__.py
│   ├── base_adapter.py          # 抽象接口定义
│   ├── maya_adapter.py          # Maya 实现
│   └── max_adapter.py           # 3ds Max 实现（后续）
│
├── core/                        # 共享核心逻辑
│   ├── __init__.py
│   ├── bridge_dcc.py            # OpenClaw 通信适配（调用 bridge_core.py，Qt signal/slot 回传）
│   ├── mcp_server.py            # MCP WebSocket 服务器（从 UE 移植，注册 DCC 特有工具）
│   ├── skill_runtime.py         # Skill 加载/执行/热重载
│   ├── context_collector.py     # DCC 上下文采集（通过 adapter 抽象）
│   ├── command_queue.py         # 主线程安全命令队列
│   ├── dependency_manager.py    # 依赖检测与自动安装
│   └── config.py                # 配置管理（端口/路径/偏好）
│
├── skills/                      # DCC 特有 Skill
│   ├── maya/                    # Maya Skill 集合
│   │   ├── scene/
│   │   ├── asset/
│   │   ├── material/
│   │   └── animation/
│   └── max/                     # Max Skill 集合（后续）
│       ├── scene/
│       ├── asset/
│       └── material/
│
├── tests/                       # 测试
│   ├── test_ui.py
│   ├── test_adapters.py
│   └── test_skills.py
│
├── resources/                   # 图标/样式资源
│   ├── icons/
│   └── styles/
│
└── README.md
```

**依赖的外部模块**（位于 `openclaw-mcp-bridge/` 目录）：
```
openclaw-mcp-bridge/
├── bridge_core.py          # Gateway WebSocket RPC 核心（平台无关，DCCClawBridge 直接 import）
├── bridge_config.py        # 配置加载
└── bridge_diagnostics.py   # 连接诊断
```

---

## 三、核心模块设计

### 3.1 DCC Adapter 抽象接口

```python
# adapters/base_adapter.py

from abc import ABC, abstractmethod
from typing import Any, Dict, List, Optional, Callable

class BaseDCCAdapter(ABC):
    """DCC 适配层抽象接口，每个 DCC 实现一个子类"""

    # ── 基础信息 ──

    @abstractmethod
    def get_software_name(self) -> str:
        """返回软件名称，如 'maya', 'max'"""

    @abstractmethod
    def get_software_version(self) -> str:
        """返回软件版本，如 '2023', '2024'"""

    @abstractmethod
    def get_python_version(self) -> str:
        """返回内置 Python 版本"""

    # ── 生命周期 ──

    @abstractmethod
    def on_startup(self) -> None:
        """DCC 启动时调用：注册菜单、shelf、启动面板"""

    @abstractmethod
    def on_shutdown(self) -> None:
        """DCC 关闭时调用：清理资源、断开连接"""

    # ── 主线程调度 ──

    @abstractmethod
    def execute_on_main_thread(self, fn: Callable, *args) -> Any:
        """在 DCC 主线程执行函数（场景 API 安全）"""

    @abstractmethod
    def execute_deferred(self, fn: Callable, *args) -> None:
        """延迟到主线程空闲时执行（非阻塞）"""

    # ── 上下文采集 ──

    @abstractmethod
    def get_selected_objects(self) -> List[Dict]:
        """获取当前选中对象列表"""

    @abstractmethod
    def get_scene_info(self) -> Dict:
        """获取当前场景基本信息（场景名、对象数、帧范围等）"""

    @abstractmethod
    def get_current_file(self) -> Optional[str]:
        """获取当前文件路径"""

    # ── UI 集成 ──

    @abstractmethod
    def get_main_window(self) -> Any:
        """获取 DCC 主窗口句柄（作为 Qt 窗口 parent）"""

    @abstractmethod
    def register_menu(self, menu_name: str, callback: Callable) -> None:
        """在 DCC 菜单栏注册入口"""

    # ── 代码执行 ──

    @abstractmethod
    def execute_code(self, code: str, context: Dict = None) -> Dict:
        """在 DCC 环境中执行 Python 代码（万能执行器）"""
```

### 3.2 Maya Adapter 实现要点

```python
# adapters/maya_adapter.py （核心骨架）

import maya.cmds as cmds
import maya.utils
import maya.OpenMayaUI as omui
from shiboken2 import wrapInstance
from PySide2 import QtWidgets

class MayaAdapter(BaseDCCAdapter):

    def get_software_name(self) -> str:
        return "maya"

    def get_software_version(self) -> str:
        return cmds.about(version=True)  # "2023"

    def execute_on_main_thread(self, fn, *args):
        return maya.utils.executeInMainThreadWithResult(fn, *args)

    def execute_deferred(self, fn, *args):
        maya.utils.executeDeferred(fn, *args)

    def get_selected_objects(self):
        sel = cmds.ls(selection=True, long=True)
        result = []
        for obj in (sel or []):
            result.append({
                "name": obj.split("|")[-1],
                "long_name": obj,
                "type": cmds.objectType(obj),
            })
        return result

    def get_main_window(self):
        ptr = omui.MQtUtil.mainWindow()
        return wrapInstance(int(ptr), QtWidgets.QMainWindow)

    def register_menu(self, menu_name, callback):
        if cmds.menu(menu_name, exists=True):
            cmds.deleteUI(menu_name)
        cmds.menu(menu_name, label="ArtClaw", parent="MayaWindow")
        cmds.menuItem(label="Open Chat Panel", command=lambda _: callback())

    def execute_code(self, code, context=None):
        # 在主线程安全执行，注入上下文变量
        ...
```

### 3.3 bridge_dcc.py — DCC 通信适配层

**已在 G3.1 完成**（`core/bridge_dcc.py`）。

与 UE 的 `openclaw_bridge.py` 对应，但使用 Qt signal/slot 替代文件轮询：

```python
from bridge_core import OpenClawBridge  # 平台无关核心（来自 openclaw-mcp-bridge/）

class DCCBridgeManager:
    """DCC 通用的 OpenClaw Bridge 管理器"""

    # Qt signals:
    #   connection_changed(bool, str)  — 连接状态变更
    #   ai_message(str, str)           — AI 流式文本 (state, text)
    #   ai_thinking(str, str)          — AI thinking 文本
    #   response_complete(str)         — 最终响应完成

    def connect(self) -> bool: ...
    def disconnect(self): ...
    def send_message(self, message: str): ...
    def cancel(self): ...
```

**会话管理（Session Key 与新对话）**：

```
Session Key 格式: agent/{client_id}:{timestamp}
  例: agent/maya-editor:1710842400
      agent/max-editor:1710842500
      agent/ue-editor:1710842600
```

- **Session Key 带时间戳**：每次新对话生成唯一 key（格式 `agent/{client_id}:{timestamp}`），Gateway 依此创建独立 session
- **新对话流程**：`reset_session()` → ① 清空 `session_key`（确保下次发送时生成新 key）② 通过 `send_message("/new")` 正常发送（走常规消息路径，AI 回复可流式显示在面板）
- **displayName 动态化**：`bridge_core.py` 内置 `_DISPLAY_NAME_MAP`，根据 `client_id` 自动映射（`maya-editor` → "Maya Claw Bridge"，`max-editor` → "Max Claw Bridge"，`ue-editor` → "UE Claw Bridge"）
- **DCC 上下文注入**：首条消息自动前缀 `[DCC Context]`（含软件名/版本/场景信息），后续消息不重复，新对话时重置

> ⚠️ **重要约束**：不能通过 bridge RPC 直接发送 `/new` slash command——Gateway 收到 `/new` 会关闭 WebSocket 连接（正常关闭 code 1000），导致需要重连。正确做法是先清空 session_key，再以普通消息方式发送 `/new`。

**Session 隔离**：

| DCC | client_id | MCP 端口 | Session 示例 |
|-----|-----------|---------|-------------|
| UE | `ue-editor` | 8080 | `agent/ue-editor:1710842400` |
| Maya | `maya-editor` | 8081 | `agent/maya-editor:1710842500` |
| Max | `max-editor` | 8082 | `agent/max-editor:1710842600` |

**与 UE 适配层的对比**：

| 方面 | UE (openclaw_bridge.py) | DCC (bridge_dcc.py) |
|------|------------------------|---------------------|
| 数据回传 | 文件轮询（C++ FTSTicker 读 JSON 文件） | Qt signal/slot（直接推送到 UI） |
| 日志 | `unreal.log` → UE Output Log | `logging` → DCC Script Editor |
| 线程模型 | 独立 asyncio 线程（相同） | 独立 asyncio 线程（相同） |
| 状态同步 | `_bridge_status.json` 文件 | Qt signal `connection_changed` |
| 依赖 | `import unreal` | `from PySide2.QtCore import Signal` |

**两者都调用同一个 `bridge_core.py`**，只是外壳不同。

### 3.4 MCP Server — 每个 DCC 独立运行

每个 DCC 进程内运行自己的 MCP Server 实例：

```
Maya 进程内:                          Max 进程内:
┌──────────────────────┐             ┌──────────────────────┐
│ MCP Server :8081     │             │ MCP Server :8082     │
│                      │             │                      │
│ 工具:                │             │ 工具:                │
│  run_python          │             │  run_python          │
│  get_selected_objects│             │  get_selected_objects│
│  get_scene_info      │             │  get_scene_info      │
│  get_node_info       │             │  get_modifier_stack  │
│  get_material_info   │             │  get_material_info   │
│  ...                 │             │  ...                 │
└──────────┬───────────┘             └──────────┬───────────┘
           │ WebSocket                          │ WebSocket
           │                                    │
    ┌──────▼────────────────────────────────────▼──────┐
    │              OpenClaw Gateway                     │
    │  mcp-bridge 插件:                                │
    │    maya-primary → ws://127.0.0.1:8081            │
    │    max-primary  → ws://127.0.0.1:8082            │
    │                                                   │
    │  AI 看到的工具名:                                 │
    │    mcp_maya-primary_run_python                    │
    │    mcp_maya-primary_get_selected_objects          │
    │    mcp_max-primary_run_python                     │
    │    mcp_max-primary_get_modifier_stack             │
    └──────────────────────────────────────────────────┘
```

**关键点**：
- 虽然 Maya 和 Max 都有 `run_python` 工具，但 AI 通过前缀 `mcp_maya-primary_` / `mcp_max-primary_` 区分
- MCP Server 代码骨架复用（JSON-RPC 协议、工具注册机制），但注册的具体工具不同
- 端口自动探测（8081+），与 UE 的 8080 错开

### 3.5 主线程命令队列

与 UEClawBridge 相同的模式：MCP/WebSocket 线程不直接调用 DCC API，而是通过命令队列转发到主线程。

```
WebSocket 线程 (MCP Server)       主线程（Qt 事件循环）
     │                                │
     │  command_queue.put(cmd)         │
     │ ──────────────────────────────► │
     │                                │  QTimer(0) 触发消费
     │                                │  adapter.execute_on_main_thread(cmd)
     │  result_future.set_result()    │
     │ ◄────────────────────────────── │
```

**Maya 特有**：Maya 的 Qt 事件循环已经是主线程，可直接用 `QTimer.singleShot(0, fn)` 或 `maya.utils.executeDeferred()` 实现。

### 3.6 通用 Qt 界面

#### Chat Panel 核心功能
- 消息列表（支持 AI 流式输出逐字显示，通过 `bridge_dcc.py` 的 Qt signal 驱动）
- 输入框（支持 / 命令补全、Shift+Enter 换行、Enter 发送）
- 代码块语法高亮 + 一键复制
- 连接状态指示器（绑定 `DCCBridgeManager.signals.connection_changed`）
- Markdown 基础渲染（加粗、列表、代码）
- **"新对话"按钮**：点击后清空 session key + 发送 `/new` 创建新会话，AI 回复流式显示在面板
- **"+ 创建技能"按钮**：绿色按钮，点击自动填充 Skill 创建提示到输入框
- **"..." 自定义快捷按钮**：用户可自定义快捷短语（格式 `名称|内容`），持久化到 `quick_inputs.json`
- **窗口标题**：显示当前 DCC 名称（如 "ArtClaw Chat - Maya"）

#### 本地命令（与 UE 保持一致）
- `/connect` `/disconnect` — 连接管理
- `/status` — 查看连接状态
- `/diagnose` — 运行健康检查
- `/clear` — 清空聊天记录
- `/cancel` — 取消当前等待
- `/help` — 命令帮助

#### 主题适配
```python
# theme.py
DCC_THEMES = {
    "maya": {
        "bg_primary": "#3C3C3C",      # Maya 深灰背景
        "bg_secondary": "#4A4A4A",
        "accent": "#5285A6",           # Maya 蓝
        "text": "#E0E0E0",
    },
    "max": {
        "bg_primary": "#3F3F3F",      # Max 深灰背景
        "bg_secondary": "#505050",
        "accent": "#4A90D9",           # Max 蓝
        "text": "#E0E0E0",
    },
}
```

---

## 四、MCP 工具设计

### 4.1 通用工具（所有 DCC 注册同名工具，但实现不同）

沿用 UEClawBridge 已验证的工具设计原则：

| 工具名 | 说明 | 类型 | 备注 |
|--------|------|------|------|
| `run_python` | 万能执行器（在 DCC 中执行 Python） | 写操作 | 各 DCC 的执行环境不同 |
| `get_editor_context` | 当前软件/版本/场景/模式 | 查询 | 返回各自的软件信息 |
| `get_selected_objects` | 获取选中对象列表 | 查询 | 返回格式统一，内容不同 |
| `get_scene_info` | 场景概览（对象数/帧范围/文件路径） | 查询 | |
| `knowledge_search` | 本地知识库检索 | 查询 | 索引各自的 API 文档 |
| `memory` | 分层记忆管理 v2（get/set/search/list/delete/check_operation/maintain） | 管理 | [v2 已实现](../../specs/记忆管理系统设计.md) |
| `skill_list` | 列出已注册 Skill | 查询 | |
| `skill_manage` | Skill 管理操作 | 管理 | |

### 4.2 Maya 特有工具

| 工具 | 说明 |
|------|------|
| `get_node_info` | 获取节点属性和连接 |
| `get_material_info` | 获取材质/着色网络信息 |
| `get_animation_info` | 获取动画曲线/关键帧信息 |

### 4.3 工具命名空间

多 DCC 实例并存时，mcp-bridge 插件自动添加 server 名称前缀：

```
AI 看到的工具名                        实际 MCP 工具名
─────────────────────────────────      ─────────────────
mcp_maya-primary_run_python         →  run_python (Maya :8081)
mcp_maya-primary_get_scene_info     →  get_scene_info (Maya :8081)
mcp_ue-editor_run_ue_python         →  run_ue_python (UE :8080)
```

---

## 五、数据流

### 5.1 用户发送消息（完整链路）

```
用户在 Chat Panel 输入
    │
    ▼
chat_panel.py 判断是否 / 命令
    │
    ├── 本地命令 → 直接执行（/clear, /status 等）
    │
    └── AI 消息 → DCCBridgeManager.send_message()
                    │
                    ▼ (独立线程)
              bridge_core.py (OpenClawBridge)
                    │ WebSocket RPC
                    ▼
              OpenClaw Gateway ──► AI Agent (LLM) 处理
                    │
              ┌─────┴────────────────────┐
              │                          │
         需要调用工具?              纯文本回复
              │                          │
              ▼                          ▼
         mcp-bridge 插件          流式 delta/final 事件
         (Gateway 内)                    │
              │ MCP JSON-RPC             │
              ▼                          │
         MCP Server (Maya内 :8081)       │
              │                          │
              ▼                          │
         command_queue.put()             │
              │                          │
              ▼ (主线程消费)             │
         adapter.execute_on_main_thread()│
              │                          │
              ▼                          │
         DCC 原生 API 执行               │
              │                          │
              ▼                          │
         结果 → Gateway → AI → 继续推理  │
              │                          │
              └──────────┬───────────────┘
                         │
                         ▼
              bridge_core.py 收到响应
                         │ Qt signal
                         ▼
              DCCBridgeManager.signals.ai_message / response_complete
                         │
                         ▼
              chat_panel.py 更新消息气泡
```

### 5.2 AI 流式响应（Qt signal 驱动）

```
OpenClaw Gateway
    │ WebSocket delta 事件
    ▼
bridge_core.py (独立 asyncio 线程)
    │ on_ai_message 回调
    ▼
bridge_dcc.py
    │ Qt signal: ai_message.emit(state, text)
    ▼
chat_panel.py (Qt 主线程, signal/slot 自动线程切换)
    │
    ▼
消息气泡逐字追加显示
```

**与 UE 的关键区别**：UE 通过文件轮询（Python 写 JSON → C++ FTSTicker 读取），DCC 通过 Qt signal（Python 直接通知 Python UI），延迟更低、代码更简洁。

---

## 六、依赖管理

### 6.1 必需依赖

| 包 | 用途 | 安装方式 |
|----|------|---------|
| `websockets` | MCP WebSocket + Gateway 通信 | pip install --target Lib/ |
| PySide2 | Qt 界面 | DCC 内置（无需安装） |

> **注意**：即使 UE 已经安装了 `websockets`，Maya 进程是独立的，需要在 Maya 的私有 `Lib/` 目录下独立安装。

### 6.2 安装策略

```
插件首次启动
    │
    ▼
检测 Lib/ 目录下是否有 websockets
    │
    ├── 有 → 正常启动
    │
    └── 无 → 后台线程 pip install --target Lib/
              │
              ▼
         安装完成 → 加载模块 → 正常启动
```

与 UEClawBridge 策略一致：安装到插件私有 `Lib/` 目录，不污染 DCC 环境。

### 6.3 Maya 2023 环境

| 项目 | 值 |
|------|----|
| Python | 3.9.7 |
| Qt | 5.15.2 (PySide2) |
| 脚本目录 | `Documents/maya/2023/scripts/` |
| 插件目录 | `Documents/maya/2023/plug-ins/` |
| userSetup.py | 自动加载入口 |

---

## 七、与 UEClawBridge 的差异对照

| 方面 | UEClawBridge | DCCClawBridge |
|------|-------------|---------------|
| 语言 | C++ + Python | 纯 Python |
| UI 框架 | Slate (UE 原生) | PySide2/Qt |
| 主线程调度 | `FTSTicker` + 文件轮询 | `QTimer` + `executeDeferred` |
| AI 响应回传 | 文件轮询（Python 写文件 → C++ 读） | Qt signal/slot（直接推送） |
| 连接状态同步 | `_bridge_status.json` 文件 | Qt signal `connection_changed` |
| 生命周期 | `UEditorSubsystem` (C++) | `userSetup.py` (Python) |
| 部署方式 | UE 插件 (.uplugin) | Python 包（复制到 scripts/） |
| 万能执行器 | `run_ue_python` (操作 `unreal` 模块) | `run_python` (操作 `maya.cmds` / `pymxs`) |
| 上下文变量 | S=选中Actor, W=World, L=unreal | S=选中对象, W=当前场景, L=cmds/pymxs |
| 平台通信适配 | `openclaw_bridge.py` (13KB) | `bridge_dcc.py` (6KB) |
| 共享核心 | `bridge_core.py` (20KB) | `bridge_core.py` (20KB) — 同一份 |

---

**文档版本**：1.2
**更新日期**：2026-03-19
**变更说明**：
- v1.2: 新增会话管理章节（Session Key 时间戳格式、新对话流程、displayName 动态化、DCC 上下文注入、/new 约束说明）；Chat Panel 功能补充新对话按钮、创建技能按钮、自定义快捷按钮、窗口标题动态化
- v1.1: 根据 Gateway 转发流程重构（G1~G4）全面调整：架构图加入双链路、bridge.py 改为 bridge_dcc.py、MCP Server 独立实例说明、数据流图更新、差异对照表增加回传方式
