"# UE 插件 UI 交互功能清单

> 记录 UE 插件 UI 展示、交互功能的完整大纲
> 基于版本: ba5907f44ef77d35ae32c3a7e2311fd20fbce48f + UI 改版 2026-03-29

---

## 一、插件入口与窗口管理

### 1.1 工具栏按钮
- 位置: UE 编辑器主工具栏 PlayToolBar 区域 + Window 菜单
- 行为: 点击打开/聚焦 Dashboard 可停靠窗口

### 1.2 可停靠窗口 (Nomad Tab)
- Tab 名称: UEClawBridgeDashboard
- Tab 类型: NomadTab（可自由停靠、拖拽）
- 显示名: ""UE Claw Bridge""

---

## 二、一体化 Dashboard 面板

### 2.1 整体布局（自上而下）

```
┌──────────────────────────────────────────────────────┐
│ 状态栏 (可折叠)                                       │
│ [已连接] v0.1.0  ●...                     [设置]     │
├──────────────────────────────────────────────────────┤
│                                                      │
│                消息列表（聊天区域）                     │
│                (SScrollBox, FillHeight)               │
│                                                      │
├──────────────────────────────────────────────────────┤
│ ▶ 快捷输入                                 [+ 添加]  │
├──────────────────────────────────────────────────────┤
│ [会话▼] [+新会话] [Skills]           [停止] [发送]   │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 输入框 (多行, 最小32px, 最大120px)               │ │
│ └──────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

### 2.2 状态栏（可折叠）
- 默认折叠，仅显示摘要行
- 展开后: 版本号、服务器地址、统计信息、操作按钮组（连接/断开/诊断/日志/发送模式）
- 状态颜色: 已连接=绿色，未连接=灰色
- 自动刷新: 每 2 秒轮询状态
- 折叠行右侧按钮:
  - **设置**: 打开设置弹窗（语言切换、发送模式、静默模式、Plan 模式、Skills 管理）
  - **版本号**: 灰色小字

### 2.3 设置面板（弹窗）
- **入口**: 状态栏右侧 [设置] 按钮
- **窗口类型**: 独立 SWindow，可重复打开（已存在则激活）
- **设置项**:
  - **语言切换**: 显示目标语言名（中文→"English"，英文→"中文"），点击切换并刷新 UI
  - **发送模式**: 复选框，勾选 = Enter 发送，取消 = Ctrl+Enter 发送
  - **静默模式**: 两个按钮
    - 中风险静默: 文件修改/移动操作自动通过（绿色=开启）
    - 高风险静默: 文件删除/批量操作自动通过（红色=开启）
  - **Plan 模式**: 开启/关闭 Plan 模式（紫色=开启）
  - **Skills 管理**: 打开 Skill 与 MCP 管理面板
  - **关闭**: 保存静默模式配置并关闭窗口

### 2.4 消息区域（聊天历史）
- 容器: SScrollBox，纵向滚动，FillHeight 占满剩余空间
- 消息上限: 500 条
- 消息模型: Sender/Content/Timestamp/bIsCode
- 发送者颜色区分: user(蓝)/assistant(绿)/streaming(灰绿)/thinking(淡紫)/system(灰)
- 本地化标签

### 2.5 快捷输入栏（可折叠，位于消息列表与输入框之间）
- **位置**: 消息列表下方、工具栏上方
- **可折叠**: 使用 SExpandableArea，默认折叠，点击标题栏展开/收起
- **显示**: 折叠时显示标题标签 "快捷输入 / Quick Inputs" 和 [+ 添加] 按钮；展开后显示快捷按钮列表
- **按钮排列**: 横向 WrapBox 自动换行，每个快捷按钮显示名称
- **操作**:
  - **点击快捷按钮**: 将对应内容填入输入框并聚焦
  - **添加 (+ 添加)**: 折叠状态下标题栏右侧，弹出模态对话框，填写名称和内容后保存
  - **编辑 (e)**: 弹出模态对话框，修改名称和内容
  - **删除 (x)**: 直接删除，无需确认
- **存储**: JSON 文件持久化 (`Saved/UEAgent/quick_inputs.json`)，UTF-8 无 BOM 编码
- **加载保障**: 双重编码兼容读取 + 加载成功后自动重写确保编码一致性
- **空状态**: 无快捷输入时仅显示标题和添加按钮

### 2.6 工具栏（消息列表下方、快捷输入上方）
- **会话切换**: 下拉菜单，显示当前会话名称，点击展开会话列表（历史会话 + 删除按钮，无新建按钮）
- **+ 新会话**: 创建全新对话会话，清空当前聊天历史
- **Skills**: 打开 Skill 与 MCP 管理面板（独立 SWindow 窗口）
- **停止**: 仅在等待 AI 回复时可点击，停止当前生成，位于工具栏右侧
- **发送**: 发送消息（等待中变灰变黄），位于工具栏最右侧
- **按钮布局**: 左侧 `[会话▼] [+新会话] [Skills]`，右侧 `[停止] [发送]`

### 2.7 输入区域
- 多行输入框（最小 32px，最大 120px）
- 发送模式切换: Enter / Ctrl+Enter（在状态栏展开区域配置）
- 动态占位提示文本
- / 前缀触发 Slash 命令菜单

---

## 三、Slash 快捷命令系统

### 3.1 命令菜单
- **触发方式**: 在输入框中输入 `/` 前缀自动弹出命令菜单
- **菜单位置**: 悬浮于输入框上方
- **实时过滤**: 根据输入内容动态过滤可用命令

### 3.2 命令列表
- **本地命令**:
  - `/connect` - 手动连接服务器
  - `/disconnect` - 断开服务器连接
  - `/diagnose` - 运行诊断检查
  - `/status` - 查看当前连接状态
  - `/clear` - 清空当前会话聊天记录
  - `/cancel` - 取消正在进行的请求
  - `/help` - 显示帮助信息
- **AI 命令**:
  - `/new` - 创建新会话
  - `/compact` - 压缩会话上下文
  - `/review` - 审阅当前操作
  - `/undo` - 撤销上一步操作

---

## 四、AI 通信与消息展示

### 4.1 消息发送流程
- 文本转义、异步发送、轮询响应

### 4.2 流式显示
- JSONL 流式文件轮询
- thinking → delta → 最终回复

### 4.3 防重发与取消
- bIsWaitingForResponse 标记
- /cancel 双端联动

### 4.4 错误处理
- 空回复、错误前缀、连接失败指引

---

## 五、连接管理

- 自动连接
- 连接状态轮询（2 秒）
- 健康检查/诊断
- MCP Server 状态检查

---

## 六、平台桥接抽象层

- IAgentPlatformBridge 接口
- FOpenClawPlatformBridge 实现
- 文件轮询通信模式

---

## 七、本地化系统

- FUEAgentL10n 运行时中英文切换
- 语言切换按钮显示**目标语言**（中文→显示 ""English""，英文→显示 ""中文""）
- 按钮/消息/输入框等文本本地化

---

## 八、编辑器子系统

- 全局状态管理
- 活跃面板追踪
- 日志分类

---

## 九、已实施优化项

- 面板合并优化
- 状态栏折叠
- 多行输入框
- 流式显示
- 发送者颜色区分
- 平台桥接抽象
- 本地化模块
- 防重发与取消
- 连接状态自动刷新
- 新对话 session 隔离
- Slash 命令分色
- MCP 状态统一
- UTF-8 安全读写
- 环境上下文自动注入
- 快捷输入 JSON 持久化
- 快捷输入增删改对话框
- 快捷输入可折叠 (SExpandableArea)
- 快捷输入双重编码兼容加载
- 快捷输入加载后自动重写确保 UTF-8 无 BOM
- 管理按钮重命名为 Skills
- Skills 按钮移至工具栏主入口
- Skills 管理面板改为独立 SWindow 窗口
- 停止按钮移至工具栏右侧
- 语言切换按钮显示目标语言

---

## 十、对话面板增强

### 10.1 消息筛选与工具调用展示
- **Session 隔离**: 按 session key 过滤事件
- **工具调用卡片**: 展示 MCP 工具（名称、参数、结果）
- **折叠/展开**: 工具信息默认折叠
- **颜色标识**: 工具名(橙)、成功(暗绿)、失败(红)

---

## 十一、会话管理

### 11.1 多会话支持
- **会话列表**: 显示所有历史会话，按时间排序
- **会话切换**: 加载历史消息（从 Gateway transcript 读取）
- **会话删除**: 删除不需要的会话
- **当前会话标识**

### 11.2 UI 设计
- 工具栏会话下拉菜单（切换/删除），不含新建按钮（新建通过工具栏的 "+ 新会话" 按钮）
- 管理弹窗（可搜索、删除）

---

## 十二、Plan 模式

### 12.1 Plan 生成与展示
- **Plan 模式开关**: 用户可选择启用 Plan 模式，AI 会先生成执行计划而非直接执行
- **AI 生成 Plan**: AI 返回 JSON 格式的执行计划，包含步骤列表和依赖关系
- **步骤卡片**: 每个步骤以卡片形式展示，状态包括:
  - 待执行（Pending）
  - 执行中（In Progress）
  - 已完成（Completed）
  - 失败（Failed）
  - 已跳过（Skipped）

### 12.2 Plan 交互
- **审阅计划**: 用户可在执行前查看完整计划，确认步骤合理性
- **编辑计划**: 可删除尚未开始执行的步骤，调整执行顺序
- **执行计划**: 逐步骤执行，实时更新每个步骤的状态
- **暂停/继续**: 支持暂停执行流程，之后可继续从暂停处开始

### 12.3 执行控制
- **暂停全部**: 暂停所有待执行步骤
- **取消全部**: 中断整个计划执行
- **步骤失败处理**: 单步骤失败时提示用户，可选择重试、跳过或终止

---

## 十三、Skill 与 MCP 管理面板

### 13.1 Skill 管理
- **分层展示**: 按来源分类显示
  - official: 官方内置 Skill
  - marketplace: 社区共享 Skill
  - user: 用户自定义 Skill
  - platform: 其他平台 Skill（不属于以上三类的统一归此）
- **操作**: 启用/禁用/钉选常用 Skill
- **筛选**:
  - 层级筛选行: 全部/官方/市集/用户/其他平台 + 未安装切换按钮
  - 软件分类筛选行: 全部/UE/Maya/Max/通用
  - 搜索框: 名称/描述模糊匹配
- **列表行**: DisplayName + Version + Author（同行）, 第二行 Skill ID
- **详情弹窗**:
  - 安装路径 + 源码路径（分别显示，路径为空时标 "-"）
  - 版本不同时显示: `v0.3.0 (源码: v0.4.0)`
  - 两个"打开目录"按钮: 打开安装目录 / 打开源码目录
- **去重**: Skill 名称别名映射（旧名→新名），frontmatter name 与目录名不一致时以目录名为准

### 13.2 Skill 生命周期
- **创建**: 通过 AI 对话式引导创建新 Skill
- **编辑**: 修改现有 Skill 的配置和代码
- **发布**: 将 Skill 发布到 marketplace 或 official 频道
- **更新**: 检查并应用 Skill 更新
- **卸载/删除**: 移除不再需要的 Skill

### 13.3 MCP 管理
- **服务器状态**: 查看各 MCP 服务器的连接状态和运行情况
- **工具列表**: 浏览当前可用的 MCP 工具，了解工具功能和参数

### 13.4 面板入口
- **主入口**: Dashboard 工具栏 [Skills] 按钮，点击打开独立 SWindow 窗口（已存在则激活）
- **备用入口**: Dashboard 状态栏 [设置] → [Skills 管理]，同样打开管理面板窗口
- **窗口类型**: 独立 SWindow（非 Nomad Tab），Dashboard 销毁时自动关闭

---

## 十四、风险确认 UI

- 风险分级提示
- 用户确认后执行

---

## 附录: 源码文件索引

| 文件 | 职责 |
|------|------|
| UEClawBridge.h/cpp | 插件模块入口 |
| UEAgentDashboard.h/cpp | 一体化面板 |
| UEAgentDashboard_Main_impl.h | 主面板 Construct 与生命周期 |
| UEAgentDashboard_StatusBar_impl.h | 状态栏辅助方法与按钮回调 |
| UEAgentDashboard_Chat_impl.h | 聊天发送与 Slash 命令处理 |
| UEAgentDashboard_MessageList_impl.h | 消息列表构建与渲染 |
| UEAgentDashboard_QuickInput_impl.h | 快捷输入增删改查与对话框 |
| UEAgentDashboard_Session_impl.h | 多会话管理与历史加载 |
| UEAgentDashboard_Plan_impl.h | Plan 模式解析与执行 |
| UEAgentDashboard_System_impl.h | 静默模式配置与环境上下文 |
| UEAgentDashboard_Settings_impl.h | 设置面板（语言/发送模式/静默/Plan/Skills） |
| UEAgentDashboard_OpenClawBridge_impl.h | OpenClaw Bridge 通信与响应处理 |
| UEAgentSubsystem.h/cpp | 编辑器子系统 |
| UEAgentLocalization.h/cpp | 本地化模块 |
| IAgentPlatformBridge.h | 平台通信接口 |
| OpenClawPlatformBridge.h/cpp | OpenClaw 实现 |
| UEAgentManagePanel.h/cpp | 管理面板入口 |
| UEAgentSkillTab.h/cpp | Skill 管理面板（UI 构建 + 行渲染） |
| UEAgentSkillTab_Data_impl.h | Skill 数据刷新与解析（Python 脚本 + JSON 解析 + 筛选） |
| UEAgentSkillTab_Actions_impl.h | Skill 操作方法（详情/启用/安装/发布等） |
| UEAgentMcpTab.h/cpp | MCP 管理面板 |

---

## 变更记录

| 日期 | 功能 | 状态 |
|------|------|------|
| 2026-03-23 | 初始版本 | 已完成 |
| 2026-03-28 | 对话框重构-消息筛选与工具调用展示 | 已完成 |
| 2026-03-28 | 多会话管理 | 已完成 |
| 2026-03-28 | Plan 模式 | 已完成 |
| 2026-03-28 | Skill/MCP 管理面板 | Phase 1-5 已完成 |
| 2026-03-29 | UI 改版: 语言切换修复、快捷输入重定位+增删改对话框、会话菜单去重、管理按钮重命名 | 已完成 |
| 2026-03-29 | 设置面板: 统一语言/静默/Plan/Skills入口，移除状态栏零散按钮和创建Skill按钮 | 已完成 |
| 2026-03-29 | 快捷输入栏可折叠 (SExpandableArea)，默认收起 | 已完成 |
| 2026-03-29 | Skills 管理按钮移至工具栏主入口，改为独立 SWindow 弹窗 | 已完成 |
| 2026-03-29 | 停止按钮移至工具栏右侧，工具栏按钮布局重排 | 已完成 |
| 2026-03-29 | 快捷输入持久化编码修复: 双重编码兼容加载 + UTF-8 无 BOM 强制写入 | 已完成 |
| 2026-03-29 | 快捷输入加载时序修复: 移至 UI 构建完成后执行 | 已完成 |
