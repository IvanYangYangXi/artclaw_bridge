# 阶段 0.5：依赖隔离与自动安装 (Dependency Management)

**目标**：在插件启动时自动检测Python环境，按需安装`websockets`、`pydantic`等必要库到插件私有目录，避免污染引擎自带的Python环境。

**🚩 里程碑**：
1. 插件启动时自动检测`site-packages`目录是否存在必要依赖
2. 缺失依赖时自动执行`pip install --target`安装到插件目录
3. 安装过程静默执行，失败时优雅降级并提供手动安装指引
4. 支持离线环境，提供预打包的依赖包
5. 所有依赖安装在`Plugins/UEClawBridge/Content/Python/Lib`目录下

---

### 1. 完整验证清单

#### 1.1 自动安装验证
- [ ] 删除`Content/Python/Lib`目录后启动编辑器
- [ ] 插件自动检测缺失依赖
- [ ] 自动执行`pip install`到Lib目录
- [ ] 安装完成后验证导入成功
- [ ] Output Log显示`✓ All dependencies installed successfully`

#### 1.2 包隔离验证
- [ ] `import websockets`成功
- [ ] `import pydantic`成功
- [ ] 检查`sys.path`包含Lib目录
- [ ] 引擎的Python环境未被修改
- [ ] 其他插件不受影响

#### 1.3 离线安装验证
- [ ] 运行`python dependency_manager.py`创建bundle
- [ ] 在无网络环境解压bundle
- [ ] 依赖正确安装到Lib目录
- [ ] 验证所有包可用

#### 1.4 错误处理验证
- [ ] 网络断开时显示友好错误提示
- [ ] 安装超时后能恢复
- [ ] 必需包安装失败时阻止插件加载
- [ ] 可选包安装失败时不影响核心功能

---

### 2. 常见问题

#### 2.1 pip命令找不到
**问题**：`python -m pip`执行失败
**解决**：
- 确保UE的Python环境包含pip
- 手动在引擎Python目录执行`python -m ensurepip`

#### 2.2 权限错误
**问题**：无法写入Lib目录
**解决**：
- 以管理员身份运行UE编辑器
- 检查插件目录的写入权限

#### 2.3 版本冲突
**问题**：与引擎自带包版本冲突
**解决**：
- 使用`--target`确保隔离安装
- 在`sys.path`中优先加载Lib目录

---

### 3. 下一步建议

**已完成**：
- ✅ 依赖自动检测机制
- ✅ 自动安装到私有目录
- ✅ 离线安装支持
- ✅ 安装验证和错误处理

**下一步（阶段 1.1）**：
进入MCP通信网关开发，实现基于WebSocket的MCP服务器，支持AI与UE编辑器的双向通信。

---

**🚩 里程碑完成标准**：
1. ✅ 插件启动时自动检测Python环境
2. ✅ 缺失依赖时自动执行pip install到Lib目录
3. ✅ 安装过程静默执行，失败时提供手动安装指引
4. ✅ 支持离线环境，提供预打包的依赖包
5. ✅ 所有依赖安装在`Plugins/UEClawBridge/Content/Python/Lib`
6. ✅ 不影响引擎和其他插件的Python环境

**提示**：
- 首次启动可能需要1-3分钟下载和安装依赖
- 建议在稳定网络环境下首次运行
- 团队部署可使用离线bundle提高效率